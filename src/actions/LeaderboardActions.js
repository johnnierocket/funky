import { SHOW_LEADERBOARD, HIDE_LEADERBOARD } from '../constants/actionTypes';

export const showLeaderboard = () => ({ type: SHOW_LEADERBOARD });
export const hideLeaderboard = () => ({ type: HIDE_LEADERBOARD });
